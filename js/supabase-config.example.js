// Supabase configuration — copy this file to supabase-config.js and fill in your credentials
// supabase-config.js is listed in .gitignore and should NEVER be committed
const SUPABASE_CONFIG = {
    url: 'YOUR_SUPABASE_URL',
    anonKey: 'YOUR_SUPABASE_ANON_KEY'
};
